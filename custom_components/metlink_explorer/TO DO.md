**Patches**
* ** Transport Map** 
  * add the live movements of trains is there a way to dynamically identify a HVL or MEL train and label 
  * add Ferry Routes
  * check if there is GPS tracking for the ferrys
  * add integrated bus routes (choose a colour and pattern that can be applied to the route number colour)
  * add GPS tracking of the busses - Is there a way to dynamically identify an 83 bus and create an icon with 83 on it

* **Bus Schedule**
  * the sensor code is not deleting the schedules once their time has passed
    * check that the actual date and time is downloaded, but only the scheduled time is displayed
    * scheduled departure time is sorted in the background

* **Dashboard Layout**
  * Can the layout of the dashboard be improved?

  **Refactor the code**
    * refactor to remove duplication of code and API calls
    * refactor to make sure that the code is dynamic as possible
    * re-analyse the API and GPS data 